# Dónde quedó todo — 2026-09-13

Sustituye a `ESTADO_2026-09-09.md`, que queda como historia. **Este es el estado
vigente.**

---

## Lo esencial en cinco líneas

1. **Los Reels están construidos y probados, pero producción todavía no los
   publica.** El Worker se desplegó por última vez el 09/09.
2. **`npm run reels` funciona de punta a punta en la máquina**: guion con Gemini,
   voz `ef_dora`, render, subida a R2 y aviso por Telegram.
3. **Ninguna pieza tiene todavía bloque `reel`**, y después del 22/09 no hay nada
   escrito.
4. `pukadigital.com` lo sigue sirviendo el Worker, y **el despliegue sigue siendo
   a mano**.
5. **La CI de GitHub no corre** desde el 02/09, por facturación: todo se verifica
   en local.

---

## Lo que pasó entre el 09/09 y hoy

| PR | Qué |
|---|---|
| #25 | cómo funciona la publicación en redes |
| #26 | la investigación de video con HyperFrames |
| #27 | el método de agentes, sincronizado con SistemaSalud |
| #28 | la spec de Reels, contrastada dos veces, y el plan 1 |
| #29 | plan 1: modelo, validación y publicación de Reels |
| #30 | lint: los 63 problemas mecánicos de `app/`, sin cambiar lo que se ve |
| #31 | plan 2, corregido con un render de verdad |
| #32 | plan 2: la producción, `npm run reels` |

Las ocho se mergearon con el check de CI en rojo, y las ocho por lo mismo: el job
falla con cero pasos ejecutados. Ver «Lo que se aprendió».

---

## Los Reels

Spec: `superpowers/specs/2026-09-13-reels-hyperframes-design.md`. Planes:
`superpowers/plans/2026-09-13-reels-publicacion.md` y
`2026-09-13-reels-produccion.md`. Cómo se producen y se publican:
`PUBLICACION_EN_REDES.md` → «Los Reels».

### Cómo quedaron

| | Plan 1 — publicación | Plan 2 — producción |
|---|---|---|
| Tasks ejecutadas por `agy` | 9 | 11 |
| Desviaciones de `agy` | 0 | 0 |
| Mutaciones comprobadas | 26 | 35 |
| Tests al cerrar | 174 | 225 |

Verificado sobre `main` después del merge del #32: 225 tests en verde, `tsc`
limpio, 7 piezas válidas, y **nada de `lib/reels/` dentro del bundle del Worker**,
con `build:cloudflare` en verde.

### La infraestructura, montada hoy

| | Dónde |
|---|---|
| Bucket de los MP4 | R2 `pukadigital-reels`. **Sin binding en `wrangler.jsonc`, a propósito**: el Worker no toca los reels |
| URL pública | `https://reels.pukadigital.com`, dominio propio del bucket |
| Guion | Gemini `gemini-3.8-flash`, con la misma clave que usa `Chatbot-IA` |
| Voz | Kokoro `ef_dora`, con su entorno en `~/.venvs/kokoro` |
| Revisión | el bot de Telegram `@Pukareelbot`, al chat privado de Luis |

Las variables están en `.env.local`; qué es cada una, en
`ENVIRONMENT_VARIABLES.md` → «Reels».

⚠️ **La `API_KEY` de Gemini también le faltaba a los captions.** `npm run
captions` lee la misma variable y, sin ella, usa su texto de respaldo.

### Probado de verdad, no solo con tests

- **Un Reel de ensayo de `crm-no-chatbot`**, renderizado, subido a R2 y descargado
  desde la URL pública: `video/mp4`, admite peticiones por rango (206), byte a
  byte igual al local, y `ffprobe` leyendo la URL da h264 · 1080×1920 · yuv420p ·
  30 fps · aac 48 kHz · 2 canales · 27,6 s.
- **Un guion real de Gemini** para la misma pieza: 5 párrafos para 5 slides,
  86 palabras, y pasó la validación.
- **Telegram recibió el video**, mandado con el mismo código que usa el comando.
  Luis lo vio y lo oyó.

### 🔴 Producción todavía no publica Reels

El último despliegue del Worker es del **2026-09-09 a las 19:10 UTC**. La
publicación de Reels entró en `main` el 13/09 con el PR #29, así que el Worker
desplegado no conoce los dos canales nuevos.

Hoy no hace daño, porque ninguna pieza trae `reel`. **Pero hay que desplegar
antes de la franja del primer Reel**: si no, no sale y no da error, igual que un
PNG sin desplegar.

Ese despliegue lleva también la regla nueva del validador —un párrafo del guion
por slide—, que no cambia nada mientras no haya bloques `reel`.

### El primer Reel, publicado a mano — 2026-09-14 01:19 UTC

`crm-no-chatbot`, con el guion del ensayo, publicado en las dos redes **llamando
desde la máquina a las mismas funciones que usa el Worker**, sin desplegar.

| | |
|---|---|
| Instagram | https://www.instagram.com/reel/DdP8JH8jEgb/ · publicado en 51 s |
| Facebook | https://www.facebook.com/reel/2114548112788234/ · procesado en menos de un minuto |

Luis lo vio en las dos. **El MP4 de R2 ya no es de ensayo: es el de este Reel.**

🔴 **Destapó un bug, ya arreglado en la rama `fix/reel-facebook-complete`.** Facebook
responde `publishing_phase: "complete"`, y el código esperaba `"completed"`: el
Reel salía y la tanda lo daba por fallido tras agotar la espera (~5 min). El plan 1
lo había «verificado contra la guía», y los fixtures copiaban esa afirmación, así
que los tests confirmaban el supuesto y no la API. Ahora el fixture es la
respuesta literal de Meta. **Hay que mergearlo antes de desplegar.**

**Si un Reel aparece en `/video_reels` mientras se procesa: sin respuesta.** Se vio
a los 40 s de empezar, y a los 59 s ya estaba todo `complete`. No hay forma de
ordenar los dos momentos, así que el aviso de no dispararlo a mano se queda.

---

## Las publicaciones

Verificado hoy contra la Graph API. Horas de Ecuador.

| Pieza | Instagram | Facebook |
|---|---|---|
| `podologo-no-receta` | ✅ 02/09 18:02 | — sin imagen propia |
| `precios-software-ecuador` | ✅ 03/09 09:43 | — sin imagen propia |
| `proteccion-datos-clinicas` | ✅ 08/09 09:02 | — sin imagen propia |
| `crm-no-chatbot` | ✅ 10/09 18:01 | ✅ **09/09 14:25**, adelantado a mano |
| `enter-tumba-factura` | 15/09 09:00 | 15/09 18:00 |
| `requisitos-facturar-sri` | 17/09 18:00 | 18/09 09:00 |
| `receta-contenido-minimo` | 22/09 09:00 | 22/09 18:00 |

`crm-no-chatbot` salió en Facebook el 09/09, al revisarlo a mano ya con su imagen
propia. Su franja del 11/09 no lo repitió: el texto ya estaba en la Página, que
es justo como se evita publicar dos veces.

⚠️ **Después del 22/09 no hay nada.** Octubre tiene temas en
`CALENDARIO_CONTENIDO.md` pero ninguna pieza escrita, y el mes no está registrado
en `content/piezas/index.ts`.

---

## Lo que se aprendió, y no está en ningún plan

**Un render de verdad encuentra lo que ningún test.** El paso 0 del plan 2
renderizó un Reel antes de dárselo a `agy`, y salieron cuatro fallos: GSAP en
línea hacía abortar `render --strict`, el error de HyperFrames sale por stdout y
no se veía, un subtítulo entero decía «reportes.», y las escenas compartían
pista. Tres se vieron leyendo el error; el del subtítulo, solo mirando el video.

**Las mutaciones cazaron dos tests en verde que no probaban nada**, y los dos los
escribió Claude al corregir el plan, no `agy`. Detalle en
`METODO_AGENTES_PARALELOS.md` §15.

**Kokoro trae su propio `espeak-ng`, y apunta a una ruta que no existe.** Viene
compilado en la CI de sus autores, con la ruta de los datos grabada apuntando
allí. Falla con `phontab: No such file or directory`, sin mencionar ni Kokoro ni
el español. Se arregla con `ESPEAK_DATA_PATH` y `PHONEMIZER_ESPEAK_LIBRARY`.

**La CI de GitHub nunca ha corrido.** La cuenta está bloqueada por facturación
desde el 02/09, y el job `verificar` sale en rojo con cero pasos y sin log. **Un
check rojo no dice nada del código.** `AGENTS.md` sigue diciendo que
`piezas --check` «es lo que corre en CI»: es la intención, no lo que pasa.

**`r2.dev` no sirve para producción.** Cloudflare lo limita y lo dice. Meta tiene
que poder descargar el MP4, así que el bucket lleva dominio propio.

---

## Lo siguiente

1. **Mergear el arreglo de `complete`** antes de cualquier despliegue con Reels.
2. **Desplegar** antes de la franja del siguiente Reel. Sin eso no sale. Si se
   quiere dejar de hacerlo a mano, Workers Builds sigue siendo la opción.
3. **Decidir si se declara «Hecho con IA» a mano.** El primer Reel salió sin la
   etiqueta en las dos redes, visto en la app el 14/09: Meta no la pone sola y la
   API no tiene parámetro.
4. **Registrar en `crm-no-chatbot` el bloque del Reel ya publicado**, con el mismo
   caption y sin fecha nueva. No publica nada —su franja ya pasó—, pero deja en el
   repositorio qué salió.
5. **Música para los Reels.** Ideas guardadas en `VIDEO_CON_HYPERFRAMES.md` →
   «Música», sin empezar ni verificar licencias.
6. **15/09 o después: apagar Vercel** (Task 14). Antes no.
7. **Escribir las piezas de octubre** y registrar el mes en
   `content/piezas/index.ts`.
8. **Resolver la facturación de GitHub**, o aceptar que no hay CI y quitar el
   workflow.
9. **Las capturas del producto.** Siguen faltando las cinco, abajo.
10. **Los 8 problemas de lint de `app/`** (3 errores, 5 avisos), que el PR #30 dejó
    fuera por no ser mecánicos.
11. **No borrar `reels/2026-09/crm-no-chatbot-3254dec0.mp4` de R2**: dejó de ser el
    ensayo y es el MP4 del Reel publicado.
12. **El token de `@Pukareelbot` se pegó en el chat de una sesión.** Si preocupa:
    `/revoke` en @BotFather y cambiarlo en `.env.local`.
13. **Reels con más movimiento: la spec está a medias.** El 14/09 se decidió,
    viéndolo animado, cómo se mueve cada elemento y cómo se organiza el código.
    Faltan el campo `foco` y las pruebas, y dos preguntas abiertas. 🔴 La más
    urgente: **la portada del Reel sale negra** —confirmado el 14/09—, porque no se
    fija y Meta toma el primer fotograma, que está vacío. Se retoma por la parte 3 de
    `superpowers/specs/2026-09-14-reels-movimiento-design.md`.
14. **`AGENTS.md` va por ~11.000 caracteres**, a menos de 1.000 del tope en que
    Antigravity lo trunca. Sacar detalle a `docs/` antes de añadir nada.

### Capturas que faltan

| Archivo | Pantalla | Tema del calendario |
|---|---|---|
| `reportes-profesionales.png` | `/reportes/profesionales` | #15, producción contra facturado |
| `agenda-dia.png` | agenda del día | #11 — ⚠️ Calendar es **unidireccional** |
| `consulta-soap.png` | consulta / historia clínica | #14, la consulta que se perdió |
| `factura-sri.png` | facturación electrónica | #3 y #21 |
| `hemodialisis-sesion.png` | sesión o CARDEX | el caso del mes de desarrollo |

Al tomarlas: recortar, nada de pantalla completa, y revisar que no se cuele el
dock de macOS ni rutas con nombres reales.

---

## Las vueltas atrás

**Si el sitio falla** — quitar las dos `routes` de `wrangler.jsonc` y
`npm run deploy:cloudflare`. El tráfico vuelve a Vercel al instante, sin tocar
DNS, mientras Vercel siga encendido. Los valores originales de DNS están en
`ESTADO_2026-09-08.md`.

**Si Facebook publica algo que no debía** — borrar el secreto `FB_PAGE_ID` y
redesplegar. El canal vuelve a `omitidos` e Instagram sigue igual.

**Si un Reel no debía salir** — quitar `reel.video` de la pieza, commitear y
desplegar, y después borrar el post. Sin `video`, el Reel no se publica. Borrar
solo el post no basta si su hora sigue abierta: volvería a salir.

**Si el cron no publica** — la ruta manual, dentro de la ventana de 60 minutos:

```bash
curl -s -H "Authorization: Bearer $(grep '^CRON_SECRET=' .env.local | cut -d= -f2-)" \
  https://pukadigital.com/api/cron/publicar
```

⚠️ Si la franja trae un Reel, **deja terminar el `curl`**: el Worker espera a que
Meta procese el video, y cortarlo lo mata a media publicación.
