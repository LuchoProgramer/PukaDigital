> ⚠️ **HISTORIA, no doctrina.** Foto del 2026-09-04. Varias cosas de aqui ya no
> son ciertas: el sitio ya no lo sirve Vercel, el cron vive en Cloudflare y los
> conteos de tests estan desfasados. **El estado vigente es
> `docs/ESTADO_2026-09-08.md`.**

# Dónde quedó todo — 2026-09-04

Traspaso de estado para la siguiente sesión. Escrito al pausar la sesión del
4 de septiembre de 2026 para retomar sin pérdida de contexto ni supuestos.

---

## 1. Estado de la Fábrica de Piezas (Fase 1)

Los componentes clave de la fábrica de estáticos están implementados y verificados:
- `lib/piezas/sistemas.ts`: Tokens visuales para los dos sistemas (`puka` Dark Glass Rojo y `health` claro/azul).
- `lib/piezas/plantilla.tsx`: Plantilla paramétrica Flexbox para Satori con soporte de todos los slots opcionales, márgenes inmutables (88px), zonas seguras de 9:16 y logos embebidos en base64 data URI.
- `scripts/muestra.ts`: Script de generación que renderiza las 6 combinaciones (2 sistemas × 3 formatos).

**Verificaciones superadas:**
- `npm test`: 14/14 tests pasando.
- `npx tsc --noEmit`: limpio (cero errores de tipos).
- `npx tsx scripts/muestra.ts`: 6 PNGs generados y guardados en `public/piezas/muestra/`.

---

## 2. Auditoría de Tracking en `/pukahealth`

Se auditó la landing de PukaHealth:
- **Píxeles y Vistas:** TikTok Pixel `trackTikTokViewContent` ($50), GA4, Meta Pixel `PageView` y Clarity.
- **CTAs:** Los 7 puntos de contacto (`pukahealth_nav`, `pukahealth_hero_primary`, `pukahealth_hero_demo`, `pukahealth_pricing_individual`, `pukahealth_pricing_anual`, `pukahealth_footer_whatsapp`, `pukahealth_footer_primary`) llaman a `ga.trackWhatsAppDirectoClick()` antes de abrir el enlace.
- **Conversiones:** Disparan Google Ads (`AW-17832260485 / 4UcUCNio2d4bEIXnirdC`), TikTok `CompleteRegistration`, GA4 `whatsapp_directo_click` y `whatsapp_opened` (vía `visibilitychange`).
- **SEO:** Schema `SoftwareApplication` + `FAQPage` + `VideoObject` renderizados en servidor vía `<SEO />`.

---

## 3. GEO y `public/llms.txt`

- **Decisión acordada:** Se mantiene `public/llms.txt` con **URLs canónicas limpias** (sin forzar parámetros UTM en las URLs del archivo).
- **Atribución de IA:**
  - Si el usuario hace clic en el enlace de la fuente en ChatGPT/Perplexity: GA4 captura el tráfico como `chatgpt.com` / `perplexity.ai`.
  - Si el usuario copia el WhatsApp directamente (Zero-Click): La atribución se realiza cualitativamente en el mensaje de bienvenida de WhatsApp (*"¿Por dónde nos encontraste?"*).

---

## 4. Correo `legal@pukadigital.com`

- Como el DNS de `pukadigital.com` está en Cloudflare, el reenvío a Gmail se realiza gratuitamente mediante **Cloudflare Email Routing**:
  1. En Cloudflare Dashboard $\rightarrow$ Email $\rightarrow$ Email Routing $\rightarrow$ Destination addresses: añadir y verificar el correo de Google.
  2. Routing rules: Crear regla `legal@pukadigital.com` $\rightarrow$ Enviar a la cuenta de Google verificada.
  3. Añadir registros DNS automáticos (MX y SPF).

---

## 5. Migración a Cloudflare Workers (Pendiente)

- **Fase A:** PR #13 (`feat/fase-a-migracion-cloudflare`) verificado (tests 80/80, `tanda.ts` modularizado).
- **Fase B (Siguiente paso):** Configuración de secretos con `wrangler secret put` y variables de build por el operador.
- **Fase C:** Apagado de cron en Vercel y activación en Cloudflare Workers entre fechas de publicación.

---

## 6. Procesos del Sistema

- Se cerró la instancia zombi de Chrome en segundo plano (`35328`) que estaba bloqueada en el puerto de depuración 9222.
